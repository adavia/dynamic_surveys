- content_for :sub_nav do
  .col-md-6
    %h1 #{current_user} - #{t("app.user.show.details")}
.container
  .row
    .col-md-8.col-md-offset-2
      .default-panel
        %h3
          = t "app.user.show.text_primary"
        = render "form"
