- content_for :sub_nav do
  .col-md-5
    %h1= t "app.survey.notifications.edit.subnav"
  .col-md-3
    %ul.list-inline
      %li
        = link_to t("app.survey.notifications.index.title"),
          [@survey, :alerts],
          class: "btn btn-sm btn-default"
      %li
        = link_to "#{@survey.customer.surveys_count} #{Survey.human_attribute_name(:class_name).downcase} total", [@survey.customer, :surveys]
.container
  .row
    .col-md-8.col-md-offset-2
      .default-panel
        %h3
          = t "app.survey.notifications.edit.text_primary"
        = render "form"
