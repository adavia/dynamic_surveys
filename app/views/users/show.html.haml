%h1.text-center.m-t-0
  = link_to "#{current_user.name} #{current_user.last_name} Profile",
    [:edit, current_user]

= render "layouts/shared/alert_messages"

= display_image(current_user.image)

%h4.text-center
  = current_user.email
